rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES - ALLOW ALL ACCESS FOR DEVELOPMENT
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
